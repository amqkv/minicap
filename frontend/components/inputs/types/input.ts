export interface InputProps {
  placeholder?: string;
  name: string;
  label: string;
  options?: string[];
  error?: boolean;
  style?: Object;
}

export interface InputPatientProps {
  label: string;
  units?: string;
  name: string;
  style?: Object;
}
